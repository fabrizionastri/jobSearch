name: Generate Codebase Diagram
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v2
      
    - name: Generate and commit diagram
      uses: githubocto/repo-visualizer@v1

